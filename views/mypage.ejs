<!doctype html>
<html lang="ko">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>마이페이지</title>

  <link rel="stylesheet" href="/style/style.css" />

  <style>
    .mypage-container {
      max-width: 900px;
      margin: 0 auto;
      padding: 140px 24px 120px;
      display: flex;
      flex-direction: column;
      gap: 36px;
      color: #fff;
    }

    h1 {
      text-align: center;
      font-size: 28px;
      margin-bottom: 10px;
    }

    /* 공통 카드 */
    .mypage-card {
      background: #1a1a1a;
      padding: 26px;
      border-radius: 14px;
      border: 1px solid #2a2a2a;
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.25);
    }

    .mypage-card h3 {
      margin-bottom: 14px;
      font-size: 17px;
      font-weight: 600;
    }

    .mypage-card input {
      width: 100%;
      padding: 12px;
      border-radius: 10px;
      border: 1px solid #333;
      background: #2a2a2a;
      color: #fff;
      font-size: 15px;
    }

    .mypage-card button {
      padding: 12px 0;
      border-radius: 10px;
      background: #fff;
      color: #111;
      font-weight: 700;
      cursor: pointer;
      margin-top: 10px;
      width: 100%;
    }

    /* 2단 그리드 */
    .mypage-row-2 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 24px;
    }

    @media (max-width: 720px) {
      .mypage-row-2 {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>

  <%- include('partials/header') %>

  <div class="mypage-container">

    <h1>마이페이지</h1>

    <!-- 닉네임 변경 -->
    <div class="mypage-card">
      <h3>닉네임 변경</h3>
      <form action="/mypage/nickname" method="POST">
        <input name="nickname" value="<%= user.nickname %>" />
        <button type="submit">저장</button>
      </form>
    </div>

    <!-- 두 가지 (아이디 / 비밀번호) -->
    <div class="mypage-row-2">

      <div class="mypage-card">
        <h3>아이디 변경</h3>
        <form action="/mypage/username" method="POST">
          <input name="username" value="<%= user.username %>" />
          <button type="submit">저장</button>
        </form>
      </div>

      <div class="mypage-card">
        <h3>비밀번호 변경</h3>
        <form action="/mypage/password" method="POST">
          <input type="password" name="oldPassword" placeholder="현재 비밀번호" />
          <input type="password" name="newPassword" placeholder="새 비밀번호" style="margin-top:8px;" />
          <button type="submit">변경</button>
        </form>
      </div>

    </div>

  </div>

  <%- include('partials/footer') %>

</body>
</html>
